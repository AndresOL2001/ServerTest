import { IBaseTemplate } from './Navbar';

const navbar: IBaseTemplate = {
  image: '/logo-blue.svg',
  item: 'MENU',
  height: 20,
  width: 30,
};

export const mockNavbarTemplateProps = {
  navbar,
};