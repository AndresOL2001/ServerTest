import { ISearchBar } from './SearchBar';

const profileCard: ISearchBar = {
  backgroundColor: '#ffff',
  hasIcon: true,
  iconColor: '#0072B5',
  borderColor: '#1067A8',
  placeHolder: 'Search'
};

export const mockNavbarTemplateProps = {
  profileCard,
};